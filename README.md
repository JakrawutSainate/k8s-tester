# 🐳 Kubernetes Beginner Lab (ฉบับภาษาไทย)

ยินดีต้อนรับสู่ Lab จำลอง Kubernetes แบบพื้นฐานสำหรับมือใหม่!
โปรเจกต์นี้ออกแบบมาให้คุณได้ลองเล่น Kubernetes (K8s) บนเครื่องตัวเอง (Windows) แบบง่ายที่สุด โดยไม่ต้องพิมพ์คำสั่งยุ่งยาก

---

## 🛠️ สิ่งที่ต้องมีในเครื่อง (Pre-requisites)
ก่อนเริ่มเล่น ต้องติดตั้งโปรแกรมเหล่านี้ก่อนนะครับ:
1.  **Docker Desktop** (ต้องรันอยู่)
2.  **Kind** (`kind.exe`) - สำหรับจำลอง Cluster
3.  **Kubectl** (`kubectl.exe`) - เครื่องมือสั่งงาน K8s

---

## 🚀 วิธีเริ่มเล่น (How to Start)

1.  เปิด **Command Prompt** หรือ **PowerShell** ในโฟลเดอร์นี้
2.  รันคำสั่ง:
    ```cmd
    START-LAB.bat
    ```
3.  นั่งจิบกาแฟรอสักครู่... ระบบจะทำการ:
    - สร้าง Cluster จำลองชื่อ `k8s-lab`
    - ลงโปรแกรมที่จำเป็น (Target App, Dashboard, Locust)
    - **เปิดหน้าเว็บให้คุณโดยอัตโนมัติ**

---

## 🖥️ หน้าเว็บที่เข้าใช้งานได้

### 1. ☸️ Kubernetes Dashboard (หน้าจัดการ K8s)
- **URL**: [https://localhost:8001](https://localhost:8001)
- **วิธีเข้า**: เลือก **Token** แล้ววางรหัสลับ (Token) ต่อยาวๆ ที่โชว์ใน Terminal ลงไป
- **ทำอะไรได้บ้าง**: ดูสถานะ Pod, ลองลบ Pod เล่นเพื่อให้มันเกิดใหม่ (Self-healing)

### 2. 🦗 Locust (หน้ายิงถล่มเว็บ / Load Testing)
- **URL**: [http://localhost:8654](http://localhost:8654)
- **Host**: `http://target-app-svc` (ตั้งไว้ให้แล้ว ไม่ต้องแก้)
- **วิธีเล่น**: ใส่จำนวน Users (เช่น 500) แล้วกด **Start Swarming** เพื่อดูว่าระบบรับไหวมั้ย

---

## 📂 โครงสร้างไฟล์ในโปรเจกต์

| ไฟล์ / โฟลเดอร์ | รายละเอียด |
| :--- | :--- |
| `START-LAB.bat` | **พระเอกของเรา** สคริปต์รันทุกอย่างอัตโนมัติ |
| `CHEAT-SHEET.md` | คู่มือการเล่น "ท่าพิสดาร" เช่น ลองลบ Pod, ลองยิงถล่ม |
| `k8s-lab/` | **โฟลเดอร์เก็บ Config ของ K8s** |
| ├── `kind-config.yaml` | ตั้งค่า Cluster (1 Master, 2 Worker Nodes) |
| ├── `target-app-deploy.yaml` | แอปเป้าหมาย (Nginx) ที่เราจะยิงทดสอบ |
| ├── `locust-deploy.yaml` | ตัวยิง Load Test (แก้ Port เป็น 8654 แล้ว) |
| └── `dashboard-setup.yaml` | ติดตั้งหน้า Dashboard และสร้าง Admin User |

---

## 🧹 วิธีเลิกเล่น (Clean Up)
เมื่อเล่นเสร็จแล้ว ให้ทำตามนี้เพื่อคืนทรัพยากรเครื่อง:
1.  ปิดหน้าต่าง Terminal ที่รัน `START-LAB.bat` (เพื่อหยุดการเชื่อมต่อ)
2.  รันคำสั่งลบ Cluster:
    ```cmd
    kind delete cluster --name k8s-lab
    ```

ขอให้สนุกกับการเรียนรู้ Kubernetes ครับ! 🎉
"# k8s-tester" 
